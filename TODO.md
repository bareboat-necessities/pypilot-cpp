# TODO

- Replace `modules/README.md` placeholder with real git submodule gitlinks if the GitHub connector exposes tree-mode 160000 updates.
- Keep submodules tracking `main` and pinned by the super-repo commit.
- Confirm umbrella CI after the first submodule pin commit.
